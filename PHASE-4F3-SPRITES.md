# Phase 4F.3 Sprite Implementation

The previous local sheet experiment is retired. Gen 9 SV front sprites are now requested directly from a deterministic CDN URL derived from the National Dex ID.

This is intentionally a small system: canonicalisation + Dex ID + one URL. The browser's normal image cache is the only runtime cache.
